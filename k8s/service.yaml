apiVersion: v1
kind: Service
metadata:
  name: canary-demo-service
  namespace: canary-demo
  labels:
    app: canary-demo
spec:
  type: LoadBalancer
  selector:
    app: canary-demo
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080